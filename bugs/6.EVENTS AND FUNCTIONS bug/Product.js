      function productNumbers(firstNumber, secondNumber) {
         let mul = firstNumber * secondNumber;
         return mul
      }
        const product = productNumbers(4, 6);
        console.log(product); 

        document.getElementById("productId").innerText = productNumbers +"\n"+"const product = productNumber(4, 6) \n" + "Answer is "+ product;